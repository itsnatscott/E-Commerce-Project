<!DOCTYPE>
<html>
    <body>
        <header>
            <a href = "/index"><center></center><img class="logo" src="/images/logo.svg"></a>
            <h2>Add A T-Shirt</h2>
            <div class="adminBar">
                <form action = "/admin" method="POST">
                    <input type = "number" name = "quant" placeholder = "Quantity">
                    <input type = "number" name = "price" placeholder = "$">
                    <input type = "text" name = "shirt_url" placeholder = "Image URL">
                    <button>Add</button>
                </form>
            </div>
        </header>
        <% if (tshirts) %>
            <ul style= "display: inline-block">
                <% tshirts.each do |shirt| %>
                    <a href="/admin/<%= shirt.id %>"><div class="shirtpic"><img src="<%= shirt.img_url %>"></div></a>

                    <div class= info>
                        <h4> quantity <%= shirt.quantity %>
                        price $<%= shirt.price %></h4>
                    </div>
                <% end %>
            </ul>
        <% end %>
    </body>
</html>